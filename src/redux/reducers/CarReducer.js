import {} from '../ACTION_TYPES';

const initialState = {}

const CarReducer = (state=initialState,action)=>{
    switch(action.type){


        default:
            return state;
    }
}
export default CarReducer;