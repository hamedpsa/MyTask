import {} from '../ACTION_TYPES';

const initialState = {}

const OtherReducer = (state=initialState,action)=>{
    switch(action.type){


        default:
            return state;
    }
}
export default OtherReducer;