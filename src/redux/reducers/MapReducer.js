import {} from '../ACTION_TYPES';

const initialState = {}

const MapReducer = (state=initialState,action)=>{
    switch(action.type){


        default:
            return state;
    }
}
export default MapReducer;