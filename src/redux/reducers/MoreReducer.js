import {} from '../ACTION_TYPES';

const initialState = {}

const MoreReducer = (state=initialState,action)=>{
    switch(action.type){


        default:
            return state;
    }
}
export default MoreReducer;