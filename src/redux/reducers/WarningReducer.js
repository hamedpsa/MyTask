import {} from '../ACTION_TYPES';

const initialState = {}

const WarningReducer = (state=initialState,action)=>{
    switch(action.type){


        default:
            return state;
    }
}
export default WarningReducer;